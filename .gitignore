node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Secrets & captured personal data — never commit
.env
.data/

# Outils/état locaux (Claude Code, préférences) — hors du repo
.claude/
.local/